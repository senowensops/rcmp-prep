import type { SectionRendererProps } from "@/types";
import { QuestionCard } from "@/components/ui/QuestionCard";
export function BusinessSection(props: SectionRendererProps) { return <QuestionCard {...props} />; }
